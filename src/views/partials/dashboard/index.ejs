<div class="container-fluid mb-5 pt-5">
    <div class="row">
        <div class="col-md-2" style="padding: 0; padding-left: 20px;">
            <% if(dashboard.show.menu){ %>
                <%- include('partials/menu') %>
            <% } %>
        </div>
        <div class="col-md-10">
            <% if(dashboard.show.import){ %>
                <%- include('partials/import') %>
            <% } else { %>
                <% if(dashboard.show.project){ %>
                    <%- include('partials/project-info') %>
                <% } %>

                <% if(dashboard.show.formLiteral){ %>
                    <%- include('partials/form-literal') %>
                <% } %>
            <% } %>

            <% if(!dashboard.show.menu){ %>
            <div class="card border-primary mb-3">
                <div class="card-body">
                    <h4 class="card-title">Inicio de la aplicación</h4>
                    <p class="card-text">Ya falta poco para poder utilizar el gestor de traducciones.</p>
                    <ul>
                        <li>Dirigase a la ruta "./import/literals/" de este proyecto.</li>
                        <li>Agrege las carpetas con sus ficheros de traducciones.</li>
                        <li>Los ficheros de traducciones deben tener extensión JSON</li>
                        <li>Los idiomas configurados son: 'ca', 'de', 'en', 'es', 'fr', 'it', 'ja', 'nl', 'pt', 'ru', 'zh'</li>
                    </ul>
                </div>
            </div>
            <% } %>
        </div>
    </div>



</div>